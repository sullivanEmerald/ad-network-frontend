"use client";
import CampaignHeader from "@/components/campaign/header";

export default function PublisherDashboardPage() {
    return (
        <div>
            <CampaignHeader title="Campaigns" description="Choose from elites campaigns" note="catch trends" />
        </div>
    );
}